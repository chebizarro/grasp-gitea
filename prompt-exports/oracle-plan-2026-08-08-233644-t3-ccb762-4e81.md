## Final Prompt
<task>Implement full GRASP-01 compliance for the grasp-gitea service.</task>
<context>
The service must be a genuine GRASP-01 service. 
Core requirements include:
1. NIP-01 relay at / accepting repository (30617) and state (30618) announcements.
2. Strict accept policy: repository announcements MUST list the service in BOTH 'clone' and 'relays' tags.
3. Relay must accept collaboration events (issues, patches, PRs) linked to accepted announcements.
4. NIP-11 document with 'supported_grasps', 'repo_acceptance_criteria', and 'curation'.
5. Git Smart HTTP at /<npub>/<identifier>.git (unauthenticated).
6. Push authorization via signed Nostr repository-state events (already partially handled by grasp-pre-receive).
7. Set repository HEAD per repo-state announcement.
8. refs/nostr/<id> lifecycle: reject on differing tip and 20-min GC of unreferenced refs.
9. Correct Git advertisements and CORS (already largely implemented).

Key files identified:
- internal/provisioner/provisioner.go (for accept policy)
- cmd/grasp-bridge/embedded_full.go (for relay logic and NIP-11)
- internal/proactivesync/service.go (for HEAD enforcement)
- internal/webhook/handler.go or similar (for refs/nostr lifecycle)

Refer to docs/reviews/grasp-compliance-audit.md for the gap analysis and grasp/01.md for the specification.
</context>

## Selection
- Files: 7 total (7 full)
- Total tokens: 16338 (Auto view)
- Token breakdown: full 16338
- Token accounting: incomplete from active_tab_published; refresh pending; incomplete: codemap_presentation

### Files
### Selected Files
├── grasp/
│   └── 01.md — 908 tokens (full)
└── grasp-gitea/
    ├── docs/
    │   └── reviews/
    │       └── grasp-compliance-audit.md — 1,657 tokens (full)
    ├── internal/
    │   ├── auth/
    │   │   ├── identity.go — 1,699 tokens (full)
    │   │   └── identity_test.go — 2,242 tokens (full)
    │   ├── config/
    │   │   └── config.go — 6,012 tokens (full)
    │   └── nip05resolve/
    │       └── resolve.go — 2,296 tokens (full)
    └── .env.example — 1,524 tokens (full)


---

## Generated Plan

## Chat Send ✅
- **Chat**: `t3-CCB762` | **Mode**: plan

### Response
# Summary
To implement full GRASP-01 compliance for the grasp-gitea service, we need to address the identified gaps in the current implementation. This involves modifying the relay accept policy, implementing the required Git Smart HTTP service, enforcing repository HEAD per repo-state announcement, and implementing the refs/nostr/<id> lifecycle. We will also need to update the NIP-11 document and ensure correct Git advertisements and CORS support.

# Current-state analysis
The current grasp-gitea service has a partial implementation of GRASP-01. The relay at `/` accepts repository and state announcements, but the accept policy is not strict, and collaboration events are not accepted. The Git Smart HTTP service is not implemented, and repository HEAD is not set per repo-state announcement. The refs/nostr/<id> lifecycle is not implemented, and the NIP-11 document is missing.

# Design
To achieve full GRASP-01 compliance, we will make the following changes:

1. **Relay accept policy**: Modify the `provisioner.go` file to enforce a strict accept policy, ensuring that repository announcements list the service in both `clone` and `relays` tags.
2. **NIP-11 document**: Update the `embedded_full.go` file to serve a NIP-11 document with the required fields: `supported_grasps`, `repo_acceptance_criteria`, and `curation`.
3. **Git Smart HTTP service**: Implement the Git Smart HTTP service at `/<npub>/<identifier>.git` in the `githttp_npub.go` file, ensuring that it is unauthenticated and accepts pushes that match the latest repo-state announcement.
4. **Repository HEAD enforcement**: Update the `proactivesync/service.go` file to set repository HEAD per repo-state announcement.
5. **refs/nostr/<id> lifecycle**: Implement the refs/nostr/<id> lifecycle in the `webhook/handler.go` file, rejecting pushes with differing tips and implementing 20-minute GC of unreferenced refs.
6. **Git advertisements and CORS**: Ensure that the Git advertisements and CORS support are correct, as already largely implemented.

# File-by-file impact
The following files will be modified:
* `internal/provisioner/provisioner.go`: Update the relay accept policy.
* `cmd/grasp-bridge/embedded_full.go`: Update the NIP-11 document and relay logic.
* `internal/proactivesync/service.go`: Update the repository HEAD enforcement.
* `internal/webhook/handler.go`: Implement the refs/nostr/<id> lifecycle.
* `internal/api/githttp_npub.go`: Implement the Git Smart HTTP service.

# Risks and migration
The changes will require careful testing to ensure that the relay accept policy and repository HEAD enforcement are working correctly. The implementation of the refs/nostr/<id> lifecycle may require additional testing to ensure that it is working as expected.

# Implementation order
The changes can be implemented in the following order:
1. Update the relay accept policy in `provisioner.go`.
2. Implement the NIP-11 document in `embedded_full.go`.
3. Implement the Git Smart HTTP service in `githttp_npub.go`.
4. Update the repository HEAD enforcement in `proactivesync/service.go`.
5. Implement the refs/nostr/<id> lifecycle in `webhook/handler.go`.
6. Test the changes thoroughly to ensure that they are working as expected.


> 💡 Continue this plan conversation with ask_oracle(chat_id: "t3-CCB762", new_chat: false)