# Phase 2 package fixtures

`alpine.apk` and `rpm.rpm` are the minimal package fixtures from Gitea v1.24.6's
`api_packages_alpine_test.go` and `api_packages_rpm_test.go` integration tests.
They are used only to verify the bridge against the exact deployed Gitea package
parsers.
